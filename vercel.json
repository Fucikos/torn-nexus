{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "client",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 10
    }
  },
  "routes": [
    {
      "src": "^/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
